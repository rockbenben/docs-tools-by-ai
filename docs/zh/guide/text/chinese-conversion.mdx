---
title: 简繁转换工具使用指南
head:
  - - meta
    - property: og:title
      content: 简繁转换工具 - 本地批量简繁体、地区词汇转换、自定义替换规则 | Tools By AI
description: 浏览器本地的中文转换工具。简繁体互转、台湾/香港地区词汇、日本新字体，支持批量文件、智能换行、自定义自定义替换规则（不被转换的特定词组），全程不上传服务器。
image: https://docs.newzone.top/img/chinese-conversion-zh.webp
howto:
  name: 用简繁转换工具的步骤
  steps:
    - name: 粘贴或上传文本
      text: 将需要转换的简体或繁体中文文本粘贴到输入区，或拖入批量 TXT 文件。
    - name: 选择转换方向
      text: 选择转换方向（简体↔台湾繁体↔香港繁体↔日本新字体）。需要保护人名/品牌名等专有词组时，先在「自定义替换规则」里写入。
    - name: 转换并复制
      text: 工具本地实时转换。复制结果即可使用，文本不会上传服务器。
faq:
  - q: 支持哪些转换方向？
    a: 6 种语言两两互转 —— 简体中文（cn）、繁体台湾（tw）、繁体台湾 + 地区词汇（twp）、繁体香港（hk）、OpenCC 模式繁体（t）、日本新字体（jp）。两个快捷按钮（繁→简、简→繁）+「自定义语言」卡片任选源/目标组合。
  - q: 「转换地区词汇」开关做什么？
    a: 控制快捷按钮用哪种繁体目标。开启 → 简↔twp（台湾词汇，把"视频"转成"影片"、"幼儿园"转成"幼稚園"）；关闭 → 简↔t（OpenCC 标准模式，仅逐字转换不动词汇）。「自定义语言」卡片不受此开关影响，直接按下拉选项执行。
  - q: 「自定义替换规则」是什么？
    a: 用户可定义"不应被转换的词组"清单 —— 例如把"软件"列入自定义替换规则，简→繁时它仍是"软件"而非"軟體"。s2t（简→繁）和 t2s（繁→简）规则各自独立。**仅在涉及简体的方向应用**（cn→x 或 x→cn），纯繁体间转换（如 tw↔hk）不应用。
  - q: 能批量转换多个文件吗？
    a: 可以。直接拖入多个 TXT 文件，结果以原文件名导出。若想限制只处理一个文件，打开「单文件模式」开关，新文件会替换当前文件。
  - q: 转换会上传到服务器吗？
    a: 不会。基于 [js-opencc](https://github.com/rockbenben/js-opencc)（[OpenCC](https://github.com/BYVoid/OpenCC) 的 JS 移植）在浏览器本地运行，所有数据都在本机处理。
  - q: 「智能换行」是做什么的？
    a: 针对 OCR 识别或 PDF 复制后常出现的"句子被错误断行"问题，根据中文标点（。？！…）、纯数字行、特殊起始符判断段落边界，重新合并段落。可独立开关。
appUrl: https://tools.newzone.top/zh/chinese-conversion
appName: Chinese Conversion
---

# 简繁转换工具

[简繁转换工具](https://tools.newzone.top/zh/chinese-conversion) 是一款本地运行的中文文本转换工具，支持简体、繁体（台湾 / 香港 / OpenCC 模式）、日本新字体之间任意方向的转换。提供两个快捷按钮（繁→简、简→繁）+ 自定义语言对 + 用户可编辑的自定义替换规则 + 批量文件处理。

![简繁转换工具界面](/img/chinese-conversion-zh.webp '简繁转换工具使用界面')

## 支持的语言代号

工具内部使用 6 个语言代号：

| 代号  | 含义                                                                                  |
| ----- | ------------------------------------------------------------------------------------- |
| `cn`  | 简体中文                                                                              |
| `tw`  | 繁体中文（台湾），仅字形转换                                                          |
| `twp` | 繁体中文（台湾）+ 地区词汇转换 —— 在 tw 基础上额外转换台湾词汇（视频↔影片、幼儿园↔幼稚園 等） |
| `hk`  | 繁体中文（香港），香港本地字形                                                        |
| `t`   | OpenCC 模式繁体，标准字形转换                                                         |
| `jp`  | 日本新字体（Shinjitai）                                                               |

## 页面布局

页面分两栏：

- **左栏**：输入区（上传 + 文本框）+ 两个快捷按钮 + 结果卡
- **右栏**：转换设置 + 自定义语言 + 自定义替换规则面板

## 快捷转换（左栏）

两个主操作按钮：

- **繁→简**：从某种繁体转到简体
- **简→繁**：从简体转到某种繁体

按钮使用哪种"繁体"由右侧「转换设置」中的**词汇转换**开关决定：

| 词汇转换开关 | 繁→简 (t2s) 等价 | 简→繁 (s2t) 等价 |
| ------------ | ---------------- | ---------------- |
| 开启         | `twp → cn`       | `cn → twp`       |
| 关闭         | `t → cn`         | `cn → t`         |

## 自定义语言（右栏）

需要 tw↔hk、t↔jp 等其他组合时使用：

1. 在「源语言」下拉中选择输入语言
2. 在「目标语言」下拉中选择输出语言
3. 点击「自定义转换」执行

⚠️ 注意：自定义替换规则只对涉及简体的转换生效（如 s2t、t2s）。选用不涉及简体的语言对（如 tw↔hk）时，已有的保护词条会被跳过，工具会显示"当前规则未生效"提示。

## 转换设置（右栏）

4 个开关：

- **词汇转换**：控制快捷按钮的繁体类型（见上表）。"自定义语言"卡片不受此开关影响
- **智能换行**：开启后根据中文标点合并断行（OCR 修复利器）
- **单文件模式**：开启后一次仅处理一个文件，新上传的文件替换当前文件
- **自动导出**：开启后转换结果直接下载为文件，不在页面显示预览（适合大批量处理）

## 自定义替换规则

工具的进阶功能，允许用户定义"不被转换"的词组清单。

### 工作机制

- **s2t 规则**：简→繁方向生效（`from` 是简体词，`to` 是希望保留的繁体写法）
- **t2s 规则**：繁→简方向生效
- 两套规则独立管理、独立存储

### 何时应用

保护规则**仅在涉及简体的转换方向**生效：

- ✅ `cn → tw/twp/hk/t/jp` → 应用 **s2t 规则**
- ✅ `tw/twp/hk/t/jp → cn` → 应用 **t2s 规则**
- ❌ `tw ↔ hk`、`t ↔ jp` 等纯繁体间转换 → 不应用规则
- ❌ 总开关关闭时 → 不应用任何规则

### 管理保护规则

点击右下角自定义替换规则面板的「管理规则」按钮，打开抽屉：

- 切换 s2t / t2s 方向 tab
- 增删改单条规则（from → to）
- 批量导入/导出（与小说排版工具共享同一份词典）

### 典型用法

- 把"机器人"加进 s2t 规则的"to"字段保留为"机器人"，避免被转成"機器人"（常见技术词）
- 把"软件"加进 s2t 规则强制保留简体写法
- 把繁体人名加入 t2s 规则避免被错误简化

## 文件上传

- 拖拽或点击上传，支持 `.txt`、`.csv`、`.json`、`.md` 等富文本格式
- 默认多文件模式，可上传一批一起处理
- 多文件状态下文本框被隐藏，处理完每个文件按原名导出

## 结果区

单文件转换完成后显示结果卡，提供：

- **复制**：一键复制结果
- **导出文件**：以原文件名（或 `jianfan.txt`）下载
- **编辑结果**：直接修改结果文本

启用「自动导出」时跳过结果显示直接下载。

## 适用场景

- 文档本地化：两岸三地内容同步（简体陆版 / 繁体台版 / 繁体港版）
- OCR 文本优化：结合智能换行修复扫描识别后的断行
- 学术 / 古籍：处理繁体文献时使用自定义替换规则保留专有名词
- 内容创作：博客、小说一键产出简繁双版本
- 批量数据：处理含中文字段的 `.json` / `.csv` / 多个 TXT

## 为什么是本地运行？

本工具基于 [js-opencc](https://github.com/rockbenben/js-opencc)（[OpenCC](https://github.com/BYVoid/OpenCC) 的 JavaScript 移植）开发，字典从 OpenCC 官方同步。完全在浏览器本地执行，文件**不上传服务器**，敏感数据可放心处理。
