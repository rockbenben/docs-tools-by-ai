---
title: JSON Sort & Classify Guide
head:
  - - meta
    - property: og:title
      content: JSON Sort & Group Tool - Custom Sort and Grouping | Tools By AI
description: Reorder messy JSON arrays. Custom order, numeric asc/desc, with an optional group field that reuses the same sort strategy inside each bucket.
appUrl: https://tools.newzone.top/en/json-sort-classify
appName: JSON Sort & Classify
image: https://docs.newzone.top/img/json-sort-classify.png
howto:
  name: How to reorder JSON arrays with JSON Sort & Group
  steps:
    - name: Paste or upload your JSON array
      text: Paste a JSON array into the input box, or drag in a file.
    - name: Pick a sort method
      text: Custom Order / Numeric Asc / Numeric Desc — pick one.
    - name: Set the sort field
      text: Specify which key to sort by (e.g. `name`, `price`).
    - name: Configure grouping (optional)
      text: Fill in the "Group field" to bucket items first; leave empty to sort the whole array.
    - name: Process
      text: Click Process. The array gets reshuffled per your rules, ready to export.
faq:
  - q: When should I pick Custom Order vs Numeric Asc/Desc?
    a: >-
      For numeric fields (price, score, count), pick Numeric Asc/Desc to sort by value. For string
      fields (name, type, status) where you want a specific order, pick Custom Order and write the
      desired sequence line-by-line in the textbox — values not listed keep their first-appearance
      order rather than getting dropped.
  - q: How do the "Group field" and "Sort field" interact?
    a: >-
      Group field is optional: leave empty to sort the whole array once; fill it and items get
      bucketed by that field (buckets ordered by first appearance), then the same sort strategy runs
      inside each bucket. Common pattern: Group field = `type`, Sort = Numeric Asc, Sort field =
      `price` gives you "items grouped by category, each category ordered cheap to expensive".
  - q: How do I group without changing in-group order?
    a: >-
      Group field = your category key; Sort strategy = Custom Order with an empty list in the textbox.
      The tool buckets items but keeps their original order inside each group. This applies the rule
      "Custom Order + empty list = preserve first-appearance order".
  - q: Does the sort field support nested paths?
    a: >-
      Yes. All field inputs accept JSONPath nested paths like `zh.name` or `stats.score`. The tool
      reads the value at that path and uses it for sorting. If you only need a few fields from a big
      JSON, use [JSON Value Extractor](./json-value-extractor.mdx) first to slim it down.
  - q: After sorting, how do I batch-edit those entries?
    a: >-
      Use [JSON Node Edit](./json-node-edit.mdx) for JSONPath-based bulk prefix/suffix, find/replace,
      or direct overwrite. All JSON tools process data locally in your browser — nothing is uploaded.
---

# JSON Sort & Group

[JSON Sort & Group](https://tools.newzone.top/en/json-sort-classify) reshuffles a JSON array by rules you pick. At its core is one single-field sort; optionally, a **group field** buckets items first and the same sort strategy runs inside each bucket — **you learn one thing, use it in two places**.

![JSON Sort & Group interface](/img/json-sort-classify.png 'JSON Sort & Group interface')

:::tip 30-second quickstart
1. Paste or upload your JSON array
2. Pick a **sort method** (Custom Order / Numeric Asc / Numeric Desc)
3. Fill in the **sort field** (which key to sort by, e.g. `name`, `price`)
4. **Group field** is optional — fill to bucket first, leave empty to sort the whole array
5. Click **Process** — array reshuffled per your rules
:::

## How it works

**Group field (optional)**

- Empty: sorts the whole array once
- Filled: buckets items by this field (buckets ordered by first appearance), then sorts inside each bucket

**Sort strategy + sort field** always apply, whether or not you grouped. Three strategies:

| Strategy | What it does |
| --- | --- |
| Custom order | Write the desired order line-by-line in the textbox. **Values not in the list — and an empty list — keep their first-appearance order** |
| Numeric asc | Smallest to largest by the sort field |
| Numeric desc | Largest to smallest by the sort field |

## Example 1: single-field custom order

```json
[{ "name": "Apple" }, { "name": "Banana" }, { "name": "Orange" }]
```

To show them as "Banana, Apple, Orange":

- Group field: leave empty
- Sort strategy: Custom order
- Sort field: `name`
- Custom order:

```
Banana
Apple
Orange
```

## Example 2: group first, sort each group by value

```json
[
  { "type": "Fruit", "price": 5 },
  { "type": "Drink", "price": 3 },
  { "type": "Fruit", "price": 2 },
  { "type": "Drink", "price": 8 }
]
```

Group by `type`, then sort each group by `price` ascending:

- Group field: `type`
- Sort strategy: Numeric asc
- Sort field: `price`

Result: Fruit comes first (it appeared first), its items ordered by price asc; then all Drink items, also asc.

## Three common patterns

- **Group only, preserve original within groups**: Group field = category key; strategy = Custom order with an empty list.
- **Group then sort by another field**: Group field = category key; strategy = Numeric asc/desc, sort field = numeric key.
- **Custom order across the whole array**: Leave group field empty; strategy = Custom order with the full list.

## Other features

- **File upload**: Drag and drop a JSON file instead of pasting.
- **JSONPath**: Field inputs accept nested paths (e.g. `zh.name`).
- **Export**: Copy or download the result.
