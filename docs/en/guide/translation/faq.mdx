---
head:
  - - meta
    - property: og:title
      content: Translation Tools FAQ - API Keys & Troubleshooting | Tools By AI
description: Translation empty or slow? Is my API key safe? This page answers common questions about our translation tools, covering API keys, DeepL, and data security.
---

# Frequently Asked Questions

## Why is the translation result empty, showing only the original text, or displaying as null?

This can happen for several reasons:

- The API Key or related settings are incorrect.
- The available credits/tokens for your account have been used up.
- The translation rate is set too high, or the API itself is temporarily unstable.

You can press F12 to open your browser's developer tools, switch to the "Network" tab, and check the API response for specific error messages.

If only some content failed to translate, simply try clicking the "Translate" button again. When the translation cache is enabled, the system will skip already translated content, avoiding duplicate charges or requests.

## What if AI translation quality isn’t ideal?

Troubleshoot in this order:

- Reset to defaults: In “Translation Settings”, reset and retest with a short sample.
- Tune temperature (default 0.7):
  - 0–0.3: technical/strict terminology (stable/consistent)
  - 0.4–0.7: general content (balanced)
  - 0.8–1.0: more creative (marketing/paraphrasing)

Note: Most mainstream online 70B+ models are fine; issues are commonly due to inappropriate temperature.

## Why is the translation speed slower? Why does the free GTX interface seem faster?

When using translation services, the speed is influenced not only by the **API response time** but also by the **translation rate settings**.

- **GTX Interface**: Generally more stable, with a higher default translation rate, which makes it appear faster in most cases.
- **AI Interface**: Has a relatively lower default translation rate. If users don’t adjust this setting, it may feel slower.

If you find the translation taking too long, you can adjust the “translation rate” in the settings.
For example, increasing it from the default value of 20 to 50 could theoretically make it about 2.5 times faster. However, keep in mind that this will also increase the number of tokens processed per translation and may even trigger the maximum token limit of the model. It’s recommended to balance speed and stability according to your actual needs.

## Why does this use a third-party interface to access DeepL?

Because DeepL's official service does not allow direct calls from a webpage, we use a "proxy channel" to send the requests for you.

This proxy interface **is only used to transmit data and will not collect any of your information**, so you can use it with confidence. If you have higher stability requirements, you can also set up this channel yourself.

## Will my API Key be saved?

No! Your API Key and other settings **are only saved in your own browser**. We do not upload or record any of your information.

## Why isn't the GTX Web interface enabled?

The GTX Web interface puts significant pressure on the server, so it is not enabled by default.

If you are using this tool on your own computer, you can enable it manually. Please avoid using this interface in a network environment with a global proxy enabled, as it may cause translation anomalies.
